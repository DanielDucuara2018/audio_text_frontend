# Audio Text Frontend - Environment Variables Template
# Copy this file to .env and fill in your values

# For development API Configuration (only app container)
# docker-compose up -d --build app
REACT_APP_AUDIO_TEXT_API_URL_ENV=http://localhost:3203
REACT_APP_AUDIO_TEXT_WS_URL_ENV=ws://localhost:3203

# For development API Configuration (app container + nginx)
# docker-compose up -d --build
# REACT_APP_AUDIO_TEXT_API_URL_ENV=https://api.voiceia.danobhub.local
# REACT_APP_AUDIO_TEXT_WS_URL_ENV=wss://api.voiceia.danobhub.local

# For production, use:
# REACT_APP_AUDIO_TEXT_API_URL_ENV=https://api.voiceia.danobhub.com
# REACT_APP_AUDIO_TEXT_WS_URL_ENV=wss://api.voiceia.danobhub.com
