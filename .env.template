# Audio Text Frontend - Environment Variables Template
# Copy this file to .env and fill in your values

# For development API Configuration (only app container)
# docker-compose up -d --build app
VITE_AUDIO_TEXT_API_URL_ENV=http://localhost:3203
VITE_AUDIO_TEXT_WS_URL_ENV=ws://localhost:3203

# For development API Configuration (app container + nginx)
# docker-compose up -d --build
# VITE_AUDIO_TEXT_API_URL_ENV=https://api.voiceia.danobhub.local
# VITE_AUDIO_TEXT_WS_URL_ENV=wss://api.voiceia.danobhub.local

# For production, use:
# VITE_AUDIO_TEXT_API_URL_ENV=https://api.voiceia.danobhub.com
# VITE_AUDIO_TEXT_WS_URL_ENV=wss://api.voiceia.danobhub.com
